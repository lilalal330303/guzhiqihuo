# 铁矿石 CTA V1.6 实施计划

## 1. 固化验收行为

- 新增 V1.6 单元测试，覆盖全周期允许做空、双周期信号、方向一致性、波动率风险档位、ATR 移动止损、显式多空仓位和换月。
- 先运行新测试，确认脚本尚未实现时按预期失败。

## 2. 实现独立聚宽脚本

- 从 V1.5 的安全执行框架复制为独立 V1.6 文件。
- 将前周期 `allow_short` 改为 `True`。
- 增加双周期信号、方向一致性和趋势状态风险乘数。
- 增加多空对称的盈利保护型 ATR 移动止损。
- 保留点时数据、锚定主连、显式多空仓位识别和安全换月。

## 3. 静态与运行验证

- 新增静态审计脚本，检查聚宽单文件边界、数据时点和关键参数。
- 用无聚宽环境的纯函数/执行桩做冒烟测试，验证前周期和后周期都能产生空头目标，风险门控能阻止不合格信号。
- 运行 V1.1-V1.6 CTA 相关测试；记录项目中与本任务无关的既有失败，不修改无关策略。

## 4. 交付

- 复制脚本到桌面策略目录，目标文件不存在时才创建，避免覆盖用户已有文件。
- 提供仓库脚本和桌面脚本路径，以及建议的聚宽分段回测顺序。
